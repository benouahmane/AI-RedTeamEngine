# LLM / agent
anthropic>=0.39.0
# NOTE: LangChain is deliberately NOT a dependency. The ReAct loop in
# agent/core.py talks to the provider API directly and never imports it — see
# ARCHITECTURE.md "Design pattern". Installing it also makes the dependency set
# unsolvable: langchain-core pins packaging>=23.2 while pwncat-cs pins
# packaging<21.0.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
jinja2>=3.1.4

# Database
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.3
alembic>=1.14.0

# Tool integrations (subprocess-based wrappers don't need libs;
# these are for tools that expose Python APIs)
pymetasploit3>=1.0.6        # MSFRPC client
impacket>=0.12.0            # AD attack toolkit
python-libnmap>=0.7.3       # alternative nmap output parser
# pwncat-cs is NOT listed here on purpose. tools/post_exploit/pwncat.py drives
# it as a subprocess (argv = ["pwncat-cs"]), so it only needs to be on PATH.
# It pins packaging<21.0, which would drag the whole venv back to 2021-era
# libraries. setup_kali.sh installs it isolated via pipx instead.
bloodhound>=1.7.0           # bloodhound-python AD collector
neo4j>=5.0.0                # Neo4j driver for BloodHound graph queries
pywinrm>=0.4.3              # WinRM client for remote Rubeus execution

# Utilities
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
structlog>=24.4.0
httpx>=0.28.0

# Dev / test
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.8.0
